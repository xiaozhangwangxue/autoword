# AutoWord official website

Static promotional website and Cloudflare Worker for `autoword.12323456.xyz`.

The Worker serves the website through Cloudflare Static Assets and streams release packages from the `autoword-downloads` R2 bucket. Device detection is performed locally in the browser and does not collect or transmit device information.

